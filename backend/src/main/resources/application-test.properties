# MySQL database URL (use your test schema)
spring.datasource.url=jdbc:mysql://localhost:3306/test_oganic
spring.datasource.username=root
spring.datasource.password=huutuan2308
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Automatically create and drop schema for test
spring.jpa.hibernate.ddl-auto=create-drop

# Use Hibernate dialect for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Show SQL for debugging (optional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
